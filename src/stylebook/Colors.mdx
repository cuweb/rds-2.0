import { Meta } from '@storybook/addon-docs/blocks';
import { ColorSwatches } from './components/ColorSwatches';

<Meta title="Stylebook/Colors" />

# Colors

Color tokens are defined in `c2b.config.json` and generate CSS custom properties
prefixed with `--rds-color-*`. Use these tokens for all color references to
maintain consistency across the design system.

<ColorSwatches
  filter="primary"
  title="Primary Colors"
  description="The primary palette and its variants."
/>

<ColorSwatches
  filter="secondary"
  title="Secondary Colors"
  description="The secondary palette and its variants."
/>

<ColorSwatches
  filter="neutral"
  title="Neutrals"
  description="Background and text colors."
/>

<ColorSwatches
  filter="semantic"
  title="Semantic Colors"
  description="Used for feedback states — success, warning, error, and info."
/>

## Usage

```css
.my-element {
  color: var(--rds--color-primary);
  background: var(--rds--color-grey-faint);
}
```
