import { Meta } from '@storybook/addon-docs/blocks';
import { HeadingScale, BodySizes, FontFamilies } from './components/TypographyScale';

<Meta title="Stylebook/Typography" />

# Typography

Typography tokens from `c2b.config.json` covering font families, the heading
scale, and responsive body text sizes.

## Font Families

<FontFamilies />

## Heading Scale

Headings use the primary color with sizes defined in `baseStyles`.

<HeadingScale />

## Body Text Sizes

Body font sizes use responsive `clamp()` values that scale between a min and max.

<BodySizes />
