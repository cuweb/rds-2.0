import { Meta } from '@storybook/addon-docs/blocks';
import { GradientSwatches } from './components/GradientSwatches';

<Meta title="Stylebook/Gradients" />

# Gradients

Gradient tokens defined in `c2b.config.json`. Each generates a CSS custom
property using its slug.

<GradientSwatches />
