import { Meta } from '@storybook/addon-docs/blocks';
import { SpacingScale } from './components/SpacingScale';

<Meta title="Stylebook/Spacing" />

# Spacing

Seven spacing levels from `2x-small` to `2x-large`. Several use viewport-relative
`min()` values for fluid scaling.

<SpacingScale />
